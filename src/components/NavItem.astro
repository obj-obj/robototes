---
export interface Props {
	href?: URL | string;
	link?: boolean;
}

const { href, link = true } = Astro.props as Props;
---

<style>
	a {
		@apply w-full lg:w-max;
	}

	a,
	p {
		@apply flex h-full items-center p-1 px-2 hover:bg-black/25;
	}
</style>

{
	link ? (
		<a href={href}>
			<slot />
		</a>
	) : (
		<p>
			<slot />
		</p>
	)
}
