{
	"name": "@example/basics",
	"version": "0.0.1",
	"private": true,
	"packageManager": "pnpm@7.0.0",
	"scripts": {
		"dev": "astro dev",
		"start": "astro dev",
		"build": "astro build && minhtml --collapse-whitespace --conservative-collapse --remove-comments --remove-empty-attributes --remove-empty-elements --remove-optional-tags --remove-redundant-attributes --use-short-doctype --minify-css --minify-js --file-ext html --input-dir dist --output-dir dist",
		"preview": "astro preview"
	},
	"devDependencies": {
		"@astrojs/sitemap": "^0.1.0",
		"@astrojs/tailwind": "^0.2.1",
		"astro": "1.0.0-beta.31",
		"minhtml": "^1.0.1",
		"sass": "^1.52.1"
	}
}
